package ch.zhaw.iwi.devops.demo;

public class StringCalc {
    public int add(String numbers) {
        if (numbers.isEmpty()) {
            return 0;
        }
        return -1;  //Basiscode
    }

}
